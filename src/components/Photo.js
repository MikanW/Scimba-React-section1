import React from "react";
import IconPhoto from "./../images/icon.png"

function Photo() {
    return(
        <div className="photoDiv">
            <img src={IconPhoto} className="iconPhoto"></img>
        </div>
    );
};

export default Photo;